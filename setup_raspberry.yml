- name: Raspberry setup
  hosts:
    - raspberry
  vars:
    motd_docker: "disabled"
    apt_unattended_upgrades: "disabled"
  remote_user: jordy
  become: true
  roles:
    - role: hostname
    - role: ssh
    - role: apt
    - role: shell
    - role: motd